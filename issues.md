# batch_rename_gui.py 问题整理

## 1. 编号功能执行时可能失效

### 问题

预览时编号逻辑正常，但实际执行重命名时可能报错。

问题代码类似：

```python
number = self.rename_options['start_number'] + self.file_paths.index(str(Path(original_name).parent / original_name))
```

`original_name` 只是文件名，不是完整路径；而 `self.file_paths` 里通常保存的是完整路径。

例如：

```text
original_name = "photo.jpg"
self.file_paths = ["/Users/mason/Desktop/photo.jpg"]
```

这会导致：

```text
ValueError: 'photo.jpg' is not in list
```

### 影响

- 使用数字编号时可能直接失败
- 预览正常，但执行出错
- 批量处理时稳定性较差

### 建议修复

不要在函数内部通过路径反查索引。  
应在循环中直接传入当前文件的 `index`。

建议结构：

```python
for index, file_path in enumerate(self.file_paths):
    new_name = self.apply_rename_rules(file_path, index)
```

然后：

```python
def apply_rename_rules(self, file_path, index):
    number = self.rename_options['start_number'] + index
```

---

## 2. 缺少撤销功能

### 问题

当前执行批量重命名后，没有撤销机制。

### 影响

如果规则设置错误，用户只能手动改回文件名。  
批量文件越多，风险越高。

### 建议改进

执行重命名前生成日志文件，例如：

```csv
old_path,new_path
/Users/mason/Desktop/a.txt,/Users/mason/Desktop/b.txt
```

增加：

- 撤销上次重命名
- 导出重命名日志
- 出错时自动回滚

---

## 3. 冲突处理不够安全

### 问题

如果目标文件名已存在，程序会自动添加 `_1`、`_2` 之类的后缀。

例如：

```text
a.txt -> b.txt
b.txt 已存在
最终可能变成 b_1.txt
```

### 影响

虽然避免了覆盖文件，但结果可能不符合用户预期。

### 建议改进

在预览阶段检测冲突：

- 目标文件已存在
- 多个文件会被改成同一个名字
- 大小写敏感/不敏感文件系统导致的冲突

如果存在冲突，应在预览中标红，并禁止执行。

---

## 4. 缺少排序控制

### 问题

批量编号时，文件顺序非常重要。  
当前主要依赖拖入顺序或系统返回顺序。

### 影响

给照片、截图、资料编号时，最终顺序可能不符合预期。

### 建议增加排序方式

支持按以下方式排序：

- 文件名
- 修改时间
- 创建时间
- 文件大小
- 文件类型
- 手动上移/下移

---

## 5. 对大量文件的稳定性一般

### 问题

当前更像轻量自用工具。  
如果一次处理几百或几千个文件，可能出现：

- UI 卡顿
- 预览刷新慢
- 错误处理不完整
- 中途失败后状态不清晰

### 建议改进

- 使用后台线程处理扫描和预览
- 增加进度条
- 增加错误列表
- 支持中止操作
- 执行前生成完整任务表

---

## 6. 缺少完整的执行前校验

### 问题

当前预览主要展示重命名结果，但不一定覆盖所有风险。

### 建议增加校验项

执行前检查：

- 新文件名是否为空
- 新文件名是否包含非法字符
- 是否和现有文件冲突
- 是否存在重复目标路径
- 是否有权限修改
- 文件是否仍然存在
- 是否跨目录移动
- 是否会改变扩展名

---

## 7. 缺少日志系统

### 问题

执行后无法追踪哪些文件成功、哪些失败。

### 建议增加日志

保存内容包括：

```text
时间
原路径
新路径
状态
错误信息
```

例如：

```csv
time,old_path,new_path,status,error
2026-05-12 12:00:00,/old/a.txt,/new/b.txt,success,
2026-05-12 12:00:01,/old/c.txt,/new/d.txt,failed,permission denied
```

---

## 8. 缺少规则预设

### 问题

用户每次都需要重新配置规则。

### 建议改进

支持保存常用规则，例如：

- 图片编号
- 截图清理
- 删除特定关键词
- 替换空格为下划线
- 统一小写
- 添加日期前缀

可以保存为 JSON：

```json
{
  "name": "照片编号",
  "prefix": "photo_",
  "start_number": 1,
  "padding": 3
}
```

---

## 9. macOS App 体验不足

### 问题

虽然使用 PyQt5 可以运行 GUI，但还不是完整 macOS 原生 App。

### 影响

用户体验可能不如正式应用：

- 没有 `.app` 包
- 没有正式图标
- 菜单栏不完整
- 文件权限处理不完善
- 无签名
- 无自动更新

### 建议改进

使用 PyInstaller 打包：

```bash
pyinstaller --windowed --name Renamica batch_rename_gui.py
```

后续可以补充：

- `.icns` 图标
- 应用签名
- DMG 安装包
- GitHub Release

---

## 10. 缺少测试

### 问题

批量重命名属于高风险操作，但目前没有测试覆盖。

### 建议增加测试

重点测试：

- 普通替换
- 正则替换
- 添加前缀
- 添加后缀
- 数字编号
- 字母编号
- 文件名冲突
- 空文件名
- 保留扩展名
- 特殊字符
- 中文文件名
- 大量文件

---

# 优先级建议

## P0：必须修

1. 修复编号执行 bug
2. 增加执行前冲突检测
3. 增加重命名日志
4. 增加撤销功能

## P1：重要改进

1. 增加排序功能
2. 增加完整预览表
3. 增加非法文件名检测
4. 增加错误列表

## P2：体验优化

1. 增加规则预设
2. 打包成 macOS `.app`
3. 增加图标
4. 增加深色/浅色自适应
5. 增加 GitHub Release

---

# 总体评价

这个脚本已经具备一个轻量批量重命名工具的基础功能：

- GUI
- 拖拽
- 预览
- 替换
- 正则
- 编号
- 前后缀

但目前还不适合处理非常重要或大量的文件。

主要原因是：

- 缺撤销
- 缺冲突预检
- 编号执行存在潜在 bug
- 缺排序
- 缺日志
- 缺测试
- 未正式打包成 macOS App

如果目标是自用，可以继续完善。  
如果目标是开源发布，建议先补齐安全机制，再做界面和打包。
